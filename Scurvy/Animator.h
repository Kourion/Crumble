#pragma once

namespace pld {
	struct Animator {
		// Collection of the frame control floats.

		// Titlescreen
		float return_key_control = 0.0f;
		float cursor_control = 0.0f;

		// Level
		float water_draw_control = 0.0f;

	};
}